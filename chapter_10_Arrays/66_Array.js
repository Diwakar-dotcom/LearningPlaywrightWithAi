let arr = [10, 20, 30, 40, 50];
console.log(arr.length);
// 0 to 3
console.log(arr[5]); // undefined

let testResults = ["pass", "fail", "skip"];
console.log(testResults);

let mixed = [1, "hello", true, null]; // we can have mixed types of variable in array unlike older languages like java, c, c++ etc.
console.log(mixed);