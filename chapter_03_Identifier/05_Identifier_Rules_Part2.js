var name = "Diwakar";

var firstName = "Diwakar";

var this_is_a_valid_identifier = "Diwakar";

var lastName = "Kumar";

// Naming Convention (Cases)

// 1. camelCase (standard way to write JS variables and functions)
let userName = "diwakar123";
let fixPrice = 99;
let isLoggedIn = true;

// 2. PascalCase (standard way to write JS classes and constructor)
let UserName = "diwakar123";
let FixPrice = 99;
let IsLoggedIn = true;

// 3. snake_case (underscore separated words)
let user_name = "diwakar123";
let fix_price = 99;
let is_logged_in = true;

// 4. SCREAMING_SNAKE_CASE (underscore separated words in uppercase, mostly used for constants)
let FIX_PRICE = 99;
let PI = 3.14159;

// 5. Hungarian Notation (prefixing variable names with a type or purpose, not commonly used in modern JS)
let strUserName = "diwakar123"; // str for string
let numFixPrice = 99; // num for number
let boolIsLoggedIn = true; // bool for boolean

// 6. kebab-case (hyphen separated words, not valid in JS variable names, but used in CSS and HTML)
// let user-name = "diwakar123"; // Invalid in JS, but valid in CSS and HTML