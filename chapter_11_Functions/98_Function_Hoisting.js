// Step 1: Definition of functions
function getUserStatus() {
    // var status_code = undefined; not shown to you

    console.log(status_code); // undefined ? - because not declared before use
    var status_code = "Active";
    console.log(status_code); // Active ? - because declared and initialised
}

// Step 2: Calling of the function
getUserStatus();